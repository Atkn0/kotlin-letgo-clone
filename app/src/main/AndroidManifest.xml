<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.productapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProductApp">
        <activity
            android:name="com.example.Views.MyListing"
            android:exported="false" />
        <activity
            android:name="com.example.Views.AddProduct"
            android:exported="false" />
        <activity
            android:name="com.example.Views.all_listing_page"
            android:exported="false" />
        <activity
            android:name="com.example.Views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>